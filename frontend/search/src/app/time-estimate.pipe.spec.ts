import { TimeEstimatePipe } from './time-estimate.pipe';

describe('TimeEstimatePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeEstimatePipe();
    expect(pipe).toBeTruthy();
  });
});
